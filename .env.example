# انسخ هذا الملف إلى .env.local وقم بملء القيم

# بيانات Supabase
# يمكنك الحصول عليها من لوحة تحكم Supabase
NEXT_PUBLIC_SUPABASE_URL=https://your-project-url.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# إعدادات الموقع
NEXT_PUBLIC_SITE_URL=https://nuqta.sa

# هذه فقط لاستخدام خدمة Supabase Admin (إذا كنت تستخدمها)
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# قم بإضافة جدول contact_messages في Supabase مع الأعمدة التالية:
# - id (int8, primary key, auto increment)
# - name (text)
# - email (text)
# - phone (text, nullable)
# - subject (text, nullable)
# - message (text)
# - created_at (timestamptz, default: now())

# قم بإضافة جدول early_access في Supabase مع الأعمدة التالية:
# - id (int8, primary key, auto increment)
# - email (text)
# - created_at (timestamptz, default: now())